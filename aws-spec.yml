version: 0.2
phases:
  build:
    commands:
      - echo Build started on `date`
      - docker --version
      - echo Building the Docker image...
      - docker build -t nest-app:latest .
artifacts:
  files: 
    - aws-spec.yml
    - dist/*